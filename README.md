# SortingAlgorithmGraphs
 
Application to display multiple different sorting algorithms by showing a graph of random distributed values and visually showing each number be sorted while the algorithm runs.